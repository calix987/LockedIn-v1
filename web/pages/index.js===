export default function Home() {
  return (
    <main style={{maxWidth: 960, margin: "48px auto", padding: "0 16px", fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial"}}>
      <h1 style={{fontSize: 32, fontWeight: 700, marginBottom: 8}}>
        Welcome to <span style={{color:"#4f46e5"}}>LockedIn</span>
      </h1>
      <p style={{color:"#374151", lineHeight: 1.6}}>
        If you can read this, the bundles → files → deploy pipeline is updating the live app.
        We’ll add auth, posts, messaging, search, and AI in the next bundles.
      </p>

      <section style={{display:"grid", gap:16, gridTemplateColumns:"repeat(auto-fit, minmax(260px, 1fr))", marginTop:24}}>
        <div style={{border:"1px solid #e5e7eb", borderRadius:12, padding:16}}>
          <h2 style={{fontWeight:600, marginBottom:8}}>Jobs</h2>
          <p style={{color:"#4b5563"}}>Search and track internships & roles.</p>
        </div>
        <div style={{border:"1px solid #e5e7eb", borderRadius:12, padding:16}}>
          <h2 style={{fontWeight:600, marginBottom:8}}>Messages</h2>
          <p style={{color:"#4b5563"}}>Real-time chat is coming in a later bundle.</p>
        </div>
      </section>
    </main>
  );
}
